# !/bin/bash

DISTRO=rhel7.3
VERSION=20.01
OMPI_ROOT=/gpfs/gpfs_stage1/${DISTRO}/OpenMPI/${VERSION}

export PATH=${OMPI_ROOT}/bin:${PATH}
export LD_LIBRARY_PATH=${OMPI_ROOT}/lib:${LD_LIBRARY_PATH}
export MANPATH=${OMPI_ROOT}/share/man:${MANPATH}
